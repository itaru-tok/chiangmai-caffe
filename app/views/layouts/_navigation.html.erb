<nav class="navbar navbar-expand-lg navbar-light bg-light custom-toggler">
  <%= link_to "ChiangMai Caffe", root_path, class: "navbar-brand", id: "logo" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li>
        <%= link_to "Digital Nomads", users_path, class: "nav-link" %>
      </li>
      <% if user_signed_in? %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Caffes
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <%= link_to caffes_path, class: "dropdown-item" do %>
              <%= fa_icon "coffee", text: "Caffe list", class: 'mx-2' %>
            <% end %>
            <%= link_to new_caffe_path, class: "dropdown-item" do %>
              <%= fa_icon "edit", text: "Create new caffe", class: 'mx-2' %>
            <% end %>
          </div>
        </li>
      <% else %>
        <li class="nav-item dropdown">
          <%= link_to caffes_path, class: "nav-link" do %>
            <%= fa_icon "coffee", text: "Caffe list", class: 'mx-2' %>
          <% end %>
        </li>
      <% end %>
      <% if user_signed_in? %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Profile
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <%= link_to edit_user_registration_path, class: "dropdown-item" do %>
              <%= fa_icon "edit", text: "Edit profile", class: 'mx-2' %>
            <% end %>
            <%= link_to current_user, class: "dropdown-item" do %>
              <%= fa_icon "user", text: "My profile", class: 'mx-2' %>
            <% end %>
          </div>
        </li>
      </ul>
    <% end %>
    <% if user_signed_in? %>
      <ul class="navbar-nav ml-auto">
        <li class='nav-item user-name'>
          <%= fa_icon 'user' %> <%= current_user.username %>
        </li>
        <li class='nav-item' >
          <%= link_to("Logout", destroy_user_session_path, class: "btn btn-xs btn-danger logout-button", method: :delete) %>
        </li>
      </ul>
    <% end %>
  </div>
</nav>
